# Copy to .env locally or use CI secrets. A free-tier key is still a secret.
# Select one tested route. Verify its live model ID and wire protocol first.
ARA_TEST_BASE_URL=
ARA_TEST_API_KEY=
OPENROUTER_API_KEY=
OPENROUTER_BASE_URL=https://openrouter.ai/api/v1
# Query the live catalogue and select an available free model before a trial.
ARA_TEST_MODEL_ID=
ARA_TEST_PROTOCOL=openai-completions
